apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-coingecko.fullname" . }}
  labels:
    {{- include "infra-coingecko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-coingecko.selectorLabels" . | nindent 4 }}
